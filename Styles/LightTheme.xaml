<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ProductApp.Converters">

    <!-- ═══════════════════════════════════════════
         BRAND COLORS (unchanged in both themes)
    ═══════════════════════════════════════════ -->
    <SolidColorBrush x:Key="PrimaryBrush"       Color="#1A237E" />
    <SolidColorBrush x:Key="PrimaryLightBrush"  Color="#534BAE" />
    <SolidColorBrush x:Key="PrimaryDarkBrush"   Color="#000051" />
    <SolidColorBrush x:Key="AccentBrush"        Color="#00897B" />
    <SolidColorBrush x:Key="SuccessBrush"       Color="#2E7D32" />
    <SolidColorBrush x:Key="WarningBrush"       Color="#F57F17" />
    <SolidColorBrush x:Key="DangerBrush"        Color="#C62828" />

    <!-- ═══════════════════════════════════════════
         LIGHT THEME SEMANTIC COLORS
    ═══════════════════════════════════════════ -->

    <!-- App / page backgrounds -->
    <SolidColorBrush x:Key="AppBackground"          Color="#F0F2F5" />
    <SolidColorBrush x:Key="CardBackground"         Color="#FFFFFF" />
    <SolidColorBrush x:Key="CardBackgroundAlt"      Color="#FAFAFA" />
    <SolidColorBrush x:Key="SurfaceBackground"      Color="#F8F9FA" />
    <SolidColorBrush x:Key="InputBackground"        Color="#FAFAFA" />
    <SolidColorBrush x:Key="InputBackgroundAlt"     Color="#F8F9FA" />

    <!-- Text -->
    <SolidColorBrush x:Key="PrimaryTextBrush"       Color="#1A237E" />
    <SolidColorBrush x:Key="HeadingTextBrush"       Color="#37474F" />
    <SolidColorBrush x:Key="BodyTextBrush"          Color="#546E7A" />
    <SolidColorBrush x:Key="SubtleTextBrush"        Color="#78909C" />
    <SolidColorBrush x:Key="MutedTextBrush"         Color="#90A4AE" />
    <SolidColorBrush x:Key="HintTextBrush"          Color="#90A4AE" />
    <SolidColorBrush x:Key="NavTextBrush"           Color="#90CAF9" />

    <!-- Borders -->
    <SolidColorBrush x:Key="BorderBrush"            Color="#E0E0E0" />
    <SolidColorBrush x:Key="BorderBrushLight"       Color="#E8E8E8" />
    <SolidColorBrush x:Key="DividerBrush"           Color="#EEEEEE" />

    <!-- Sidebar -->
    <SolidColorBrush x:Key="SidebarBackground"      Color="#1A237E" />
    <SolidColorBrush x:Key="SidebarDivider"         Color="#FFFFFF" />
    <Color            x:Key="SidebarDividerOpacity">#1AFFFFFF</Color>

    <!-- Overlay / Modal -->
    <SolidColorBrush x:Key="OverlayCardBackground"  Color="#FFFFFF" />
    <SolidColorBrush x:Key="OverlayFooterBrush"     Color="#FAFAFA" />
    <SolidColorBrush x:Key="OverlayFooterBorder"    Color="#E8E8E8" />
    <SolidColorBrush x:Key="DialogHeaderBrush"      Color="#FFFFFF" />

    <!-- Buttons -->
    <SolidColorBrush x:Key="SecondaryButtonBg"      Color="#F5F5F5" />
    <SolidColorBrush x:Key="SecondaryButtonFg"      Color="#546E7A" />
    <SolidColorBrush x:Key="SecondaryButtonBorder"  Color="#E0E0E0" />

    <!-- Theme toggle icon colors -->
    <SolidColorBrush x:Key="ThemeToggleFg"          Color="#90CAF9" />

    <!-- ═══════════════════════════════════════════
         INVOICE BADGE COLORS (hex-keyed so code-behind
         pastel chips also follow the active theme)
    ═══════════════════════════════════════════ -->
    <SolidColorBrush x:Key="#E8F5E9" Color="#E8F5E9" />
    <SolidColorBrush x:Key="#2E7D32" Color="#2E7D32" />
    <SolidColorBrush x:Key="#FFF8E1" Color="#FFF8E1" />
    <SolidColorBrush x:Key="#F57F17" Color="#F57F17" />
    <SolidColorBrush x:Key="#F5F5F5" Color="#F5F5F5" />
    <SolidColorBrush x:Key="#9E9E9E" Color="#9E9E9E" />
    <SolidColorBrush x:Key="#FFEBEE" Color="#FFEBEE" />
    <SolidColorBrush x:Key="#C62828" Color="#C62828" />
    <SolidColorBrush x:Key="#E8EAF6" Color="#E8EAF6" />
    <SolidColorBrush x:Key="#1A237E" Color="#1A237E" />
    <SolidColorBrush x:Key="#FFF3E0" Color="#FFF3E0" />
    <SolidColorBrush x:Key="#E65100" Color="#E65100" />
    <SolidColorBrush x:Key="#E0F2F1" Color="#E0F2F1" />
    <SolidColorBrush x:Key="#00695C" Color="#00695C" />
    <SolidColorBrush x:Key="#F0F0FF" Color="#F0F0FF" />
    <SolidColorBrush x:Key="#5C6BC0" Color="#5C6BC0" />
    <SolidColorBrush x:Key="#283593" Color="#283593" />
    <SolidColorBrush x:Key="#1B5E20" Color="#1B5E20" />
    <SolidColorBrush x:Key="#90A4AE" Color="#90A4AE" />
    <SolidColorBrush x:Key="#BDBDBD" Color="#BDBDBD" />
    <SolidColorBrush x:Key="BadgeInfoBg"          Color="#E8EAF6" />
    <SolidColorBrush x:Key="BadgeInfoFg"          Color="#1A237E" />
    <SolidColorBrush x:Key="BadgePurpleBg"        Color="#F3E5F5" />
    <SolidColorBrush x:Key="BadgePurpleBg2"       Color="#E1BEE7" />
    <SolidColorBrush x:Key="BadgePurpleFg"        Color="#7B1FA2" />
    <SolidColorBrush x:Key="BadgePurpleBorder"    Color="#CE93D8" />

    <!-- ═══════════════════════════════════════════
         SCROLLBAR &amp; SCROLLVIEWER
    ═══════════════════════════════════════════ -->
    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="8" />
        <Setter Property="MinWidth" Value="8" />
    </Style>

    <Style TargetType="ScrollViewer">
        <Setter Property="local:ScrollBehavior.ForceMouseWheel" Value="True" />
    </Style>

    <!-- ═══════════════════════════════════════════
         GLOBAL CONTROL STYLES
    ═══════════════════════════════════════════ -->

<!-- Global TextBox style — ensures text is always visible in both themes -->
    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{DynamicResource HeadingTextBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource HeadingTextBrush}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource PrimaryBrush}" />
        <Setter Property="Background" Value="{DynamicResource InputBackground}" />
    </Style>

    <!-- TabControl + TabItem — theme-friendly headers, no default white chrome -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>
    <Style TargetType="TabItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource BodyTextBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="20,9" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="10,10,0,0" Margin="2,5,2,0">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceBackground}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                            <Setter TargetName="Bd" Property="BorderThickness" Value="0,0,0,2" />
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#1A000000" />
                            <Setter Property="Foreground" Value="{DynamicResource HeadingTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="Background" Value="#1A237E" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="#00897B" />
    </Style>

    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackground}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.12" Color="#000000" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PageTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="Margin" Value="0,0,0,16" />
    </Style>

    <Style x:Key="SectionTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource HeadingTextBrush}" />
        <Setter Property="Margin" Value="0,12,0,8" />
    </Style>

    <Style x:Key="FormField" TargetType="Control">
        <Setter Property="Padding" Value="8" />
        <Setter Property="Margin" Value="0,0,0,8" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="Background" Value="{DynamicResource InputBackground}" />
    </Style>
    <Style x:Key="FormFieldPassword" TargetType="PasswordBox" BasedOn="{StaticResource FormField}" />

    <!-- ═══════════════════════════════════════════
         LIGHT COMBOBOX — full custom template
         Mirrors dark theme template for consistency
    ═══════════════════════════════════════════ -->

    <!-- Dropdown item style -->
    <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
        <Setter Property="Background"   Value="Transparent" />
        <Setter Property="Foreground"   Value="{DynamicResource HeadingTextBrush}" />
        <Setter Property="Padding"      Value="12,8" />
        <Setter Property="FontSize"     Value="13" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="4,2">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceBackground}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource InputBackground}" />
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Global ComboBox style for light mode -->
    <Style TargetType="ComboBox">
        <Setter Property="Background"   Value="{DynamicResource InputBackground}" />
        <Setter Property="Foreground"   Value="{DynamicResource HeadingTextBrush}" />
        <Setter Property="BorderBrush"  Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding"      Value="10,6" />
        <Setter Property="FontSize"     Value="13" />
        <Setter Property="ItemContainerStyle" Value="{DynamicResource DarkComboBoxItem}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <!-- Main box -->
                        <Border x:Name="MainBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="32" />
                                </Grid.ColumnDefinitions>
                                <!-- Selected item area -->
                                <ContentPresenter x:Name="ContentSite"
                                                  Grid.Column="0"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Stretch"
                                                  IsHitTestVisible="False" />
                                <!-- Chevron arrow -->
                                <Path Grid.Column="1"
                                      FlowDirection="LeftToRight"
                                      Width="10" Height="6"
                                      Fill="{DynamicResource BodyTextBrush}"
                                      Stretch="Uniform"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Data="M7 10l5 5 5-5z" />
                            </Grid>
                        </Border>
                        <!-- Invisible toggle button to handle clicks -->
                        <ToggleButton x:Name="ToggleButton"
                                      Background="Transparent" BorderThickness="0"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                  RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press" />
                        <!-- Dropdown popup -->
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{DynamicResource CardBackground}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="12" ShadowDepth="3"
                                                          Opacity="0.15" Color="#000000" />
                                    </Border.Effect>
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush"
                                    Value="{DynamicResource PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush"
                                    Value="{DynamicResource PrimaryBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
